# PRD: 카테고리 기반 국내 지도 앱

## 1. 개요

| 항목 | 내용 |
| --- | --- |
| 제품명 | 카테고리 기반 국내 지도 앱 (가칭) |
| 한 줄 설명 | 카테고리를 선택하면 해당 장소만 지도에 표시하고, 실제로 이동 가능한 가장 빠른 경로와 예상 소요 시간을 안내하는 국내 지도 웹앱 |
| 대상 플랫폼 | 웹(모바일·데스크톱 반응형), 초기 개발·최적화는 데스크톱 우선 |
| 문서 상태 | 초안 (1단계 개발 착수 전) |

## 2. 배경 및 문제 정의

기존 지도 서비스(카카오맵, 네이버지도, 티맵 등)는 정보량이 많아 사용자가 원하는 장소 유형만 빠르게 비교하기 어렵다. 이 제품은 카테고리를 먼저 선택하게 하여 지도를 단순화하고, 선택한 장소까지의 이동 방법을 빠르게 비교할 수 있도록 한다.

### 2.1 차별화 포인트와 리스크

- 기존 앱도 카테고리 필터와 길찾기를 이미 제공하므로, 본 제품은 범용 지도가 아니라 "카테고리 선택 후 3번 이내 조작으로 결과를 본다"는 좁고 빠른 흐름 하나에 집중한다.
- 기존 앱 대비 우위가 실제로 체감되는지는 검증되지 않은 가설이므로, 1단계 프로토타입 단계에서 소수 사용자 테스트로 조기에 확인한다. 우위가 확인되지 않으면 방향을 재검토한다.

## 3. 목표

- 사용자가 카테고리 선택만으로 원하는 장소를 지도에서 즉시 좁혀볼 수 있게 한다.
- 선택한 장소까지 도보로 이동 가능한 경로와 예상 시간을 안내한다.
- 이후 대중교통 경로까지 확장 가능한 구조를 마련한다.

### 3.1 목표가 아닌 것 (Out of Scope, MVP 기준)

- 버스·지하철 등 대중교통 경로 (2단계 이후)
- 실시간 배차 정보 (2단계 이후)
- 복수 카테고리 동시 선택 (4단계)
- 즐겨찾기, 최근 검색 등 개인화 기능 (4단계)
- 해외 지역 지원

## 4. 핵심 사용자와 사용 상황

- 주변 편의점, 식당, 카페 등을 빠르게 찾고 싶은 사용자
- 목적지까지 도보, 버스, 지하철 중 가장 빠른 방법을 비교하려는 사용자
- 지도에 표시되는 정보가 너무 많아 필요한 장소만 보고 싶은 사용자
- 국내 여행이나 일상적인 이동 계획을 세우는 사용자

## 5. 핵심 사용자 흐름

1. 사용자가 현재 위치 또는 출발지를 설정한다.
2. 지도에서 카테고리를 선택한다.
3. 선택한 카테고리에 해당하는 장소만 지도에 표시된다.
4. 장소 마커를 선택하면 장소명, 주소, 거리, 운영 정보 등 기본 정보를 확인한다.
5. 출발지와 목적지를 설정하고 이동 경로를 요청한다.
6. (2단계 이후) 도보, 버스, 지하철, 복합 경로별 예상 소요 시간을 비교한다.
7. 가장 빠른 경로를 기본 추천하고, 선택한 경로를 지도에 표시한다.

## 6. 카테고리 정의

초기 카테고리는 다음 8개로 구성한다.

| 카테고리 | 조회 방식 |
| --- | --- |
| 편의점 | 카테고리 코드 검색 |
| 식당 | 카테고리 코드 검색 |
| 마트 | 카테고리 코드 검색 |
| 학교 | 카테고리 코드 검색 |
| 카페 | 카테고리 코드 검색 |
| PC방 | 카테고리 코드 없음 → 키워드 검색("PC방")으로 대체 |
| 영화관 | 카테고리 코드 검색 |
| 공원 | 카테고리 코드 검색 |

- 카테고리는 여러 개를 동시에 선택할 수 있도록 향후 확장 가능하지만, MVP에서는 한 번에 하나만 선택한다.
- 카테고리별 정확한 조회 방식(코드/키워드)은 1단계 개발 시 선정한 API 기준으로 확정한다.

## 7. 기능 요구사항

### 7.1 지도 및 장소 탐색

- FR-1: 국내 지역을 대상으로 지도를 표시한다.
- FR-2: 확대·축소, 지도 이동, 현재 위치 확인을 지원한다.
- FR-3: 카테고리 선택 시 해당 카테고리의 장소를 자동으로 검색한다.
- FR-4: 선택한 카테고리의 장소만 지도에 표시하고 나머지는 숨긴다.
- FR-5: 지도 이동 또는 확대·축소 후 현재 화면 영역의 장소를 다시 조회한다.
- FR-6: 장소 마커 선택 시 장소명, 주소, 거리, 운영 시간 등 상세 정보를 표시한다.
- FR-7: 키워드로 장소를 검색할 수 있다.

### 7.2 지도 디자인

- FR-8: 물, 도로, 공원, 건물 영역을 서로 다른 색으로 구분한다.
- FR-9: 도로와 보행로를 구분해 실제 지도처럼 정보를 읽을 수 있어야 한다.
- FR-10: 카테고리별 마커 색상 또는 아이콘을 다르게 표시한다.
- FR-11: 경로가 장소 마커와 겹치더라도 식별 가능하도록 경로 색상과 강조 상태를 제공한다.

### 7.3 경로 안내

- FR-12: 출발지와 목적지를 설정할 수 있다.
- FR-13: 사람이 이동 가능한 보행로를 기준으로 도보 경로를 계산한다 (MVP).
- FR-14: 가능한 경로 중 예상 시간이 가장 짧은 경로를 우선 추천한다.
- FR-15: (2단계) 도보, 버스, 지하철, 복합 경로를 제공한다.
- FR-16: 경로별 총 소요 시간, 이동 거리, 환승 횟수, 도보 시간을 표시한다.
- FR-17: 선택한 경로를 지도에 선으로 표시하고 단계별 이동 안내를 제공한다.
- FR-18: (2단계) 버스 경로에는 버스 번호를, 지하철 경로에는 노선명과 승하차 역을 표시한다.
- FR-19: (2단계) 대중교통 예상 시간에는 도보 시간, 대기 시간, 이동 시간, 환승 시간을 포함한다.
- FR-20: 실시간 교통 데이터가 없거나 조회에 실패하면 마지막 갱신 시각 또는 일반적인 예상값임을 표시한다.

## 8. MVP 범위

첫 번째 버전은 다음 범위로 제한한다.

- 국내 지도 표시 및 확대·축소
- 현재 위치 또는 검색한 위치를 중심으로 지도 이동
- 8개 카테고리 중 하나 선택
- 선택 카테고리 장소의 마커 표시 및 기본 상세 정보
- 장소명과 주소 기반 검색
- 출발지와 목적지 사이의 도보 경로
- 도보 예상 거리와 소요 시간

버스·지하철 경로, 실시간 배차 정보, 복수 카테고리 선택은 MVP 검증 후 2단계에서 추가한다. 이를 통해 "필요한 장소만 빠르게 찾는 경험"이 실제로 유용한지 먼저 검증한다.

## 9. 단계별 개발 계획

### 1단계: 지도와 장소 필터

- 지도·경로 API PoC 및 선정 (본 문서 11장 참고, 착수 조건)
- 지도 SDK 연동
- 국내 초기 화면 및 기본 지도 스타일 구성
- 카테고리 선택 UI 구현
- 카테고리별 장소 검색 및 마커 표시
- 마커 클릭 상세 패널 구현

### 2단계: 검색과 도보 경로

- 장소명·주소 검색
- 출발지·목적지 설정
- 보행 가능 경로 조회
- 최단 예상 시간 경로 추천
- 거리·시간·단계별 안내 표시

### 3단계: 대중교통 경로

- 버스 및 지하철 경로 조회
- 버스 번호, 지하철 노선, 환승 정보 표시
- 배차 및 대기 시간을 포함한 예상 시간 계산
- 도보·버스·지하철 복합 경로 비교

### 4단계: 사용성 개선

- 복수 카테고리 선택
- 즐겨찾기와 최근 검색
- 모바일 화면 최적화
- 장소 정보와 교통 정보의 갱신 시각 표시
- 경로 재검색 및 오류 상태 개선

## 10. 주요 화면

- 지도 화면: 지도, 검색창, 카테고리 버튼, 현재 위치 버튼, 확대·축소 버튼
- 장소 결과 패널: 선택 카테고리의 장소 목록과 지도 마커 연동
- 장소 상세 패널: 장소 정보와 길찾기 시작 버튼
- 경로 비교 패널: 이동 수단별 시간, 거리, 환승 정보
- 경로 상세 화면: 선택 경로의 지도 표시와 단계별 안내

## 11. 데이터·기술 요구사항 및 제약

- 지도 표시·장소 검색과 도보 경로 계산은 서로 다른 제공사의 API가 필요할 가능성이 높다. 국내 지도 API(Kakao Maps, Naver Maps 등)는 지도 렌더링과 장소 검색은 지원하지만 도보(보행자) 길찾기 API를 공개 제공하지 않는 경우가 많으므로, 도보 경로 전용 API(예: Tmap 보행자 경로, ODsay 등)를 별도로 선정해야 한다.
- **착수 조건(Blocking):** 후보 API 조합(지도/장소 검색용 API + 도보 경로용 API)으로 실제 호출 테스트(PoC)를 1단계 개발 착수 전에 먼저 수행해 무료 호출 한도·응답 품질·지도 위 경로 표시 가능 여부를 확인한다. 확인 전에는 1단계 구현에 착수하지 않는다.
- 선정 후보 API들의 이용약관을 확인해 자체 길찾기·경로 안내 기능 제공이 약관상 허용되는지 점검한다(재판매·경쟁 서비스 제한 조항 확인).
- 국내 주소와 장소 데이터의 정확도 및 호출량 제한을 확인한다.
- 위치 정보 사용 시 사용자 동의와 권한 거부 상태를 처리한다.
- API 키가 클라이언트 코드에 노출되지 않도록 서버 또는 환경 변수로 관리한다.
- 실시간 배차 정보가 제공되지 않는 지역에 대한 대체 표시를 정의한다.
- 장소 데이터의 중복, 폐업, 운영 시간 변경 처리 방식을 정한다.

## 12. 성공 기준 (Acceptance Criteria)

- 사용자가 3번 이내의 조작으로 원하는 카테고리의 주변 장소를 확인할 수 있다.
- 카테고리를 바꾸면 이전 카테고리의 장소가 지도에서 숨겨지고 새 장소만 표시된다.
- 장소 상세 정보에서 길찾기를 바로 시작할 수 있다.
- 선정한 도보 경로 API가 보행 불가능한 도로(자동차 전용도로 등)를 결과에 포함하지 않는지 실제 경로 샘플로 검증한다.
- 경로 결과에 거리와 예상 시간이 일관되게 표시된다.
- 모바일과 데스크톱에서 지도, 마커, 상세 패널이 겹치지 않고 사용할 수 있다.

## 13. 결정된 사항

- 모바일과 데스크톱을 모두 지원하는 것을 목표로 하며, 초기 개발과 최적화는 데스크톱 환경을 우선한다.
- 지도 표시·장소 검색과 도보 경로 계산은 별도 API 제공사 조합(예: 지도/장소 검색용 API + 도보 경로 전용 API)으로 구성될 수 있다고 전제하고 설계한다.
- 선정하려는 지도·경로 API의 이용약관(재판매·경쟁 서비스 제한 여부)을 1단계 착수 전에 확인한다.

## 14. 미해결 사항 (Open Questions)

- 사용할 지도 및 장소 검색 API (지도 렌더링·카테고리/키워드 검색을 지원하는 제공사 선정)
- 사용할 도보 경로 계산 API (지도 API와 별도 제공사일 가능성이 높으므로 우선순위를 두고 검토)
- 사용할 대중교통 API와 비용 한도 (2단계 대상)
- 현재 위치를 기본 출발지로 자동 설정할지 여부
- 장소 운영 시간과 전화번호 등 상세 정보의 제공 범위
- 실시간 교통 정보가 없을 때의 안내 문구
- 기존 지도 앱 대비 차별점을 어떤 지표(예: 조작 횟수, 재방문율)로 검증할지

지도·장소·경로 API 선정 항목은 미결로 남겨두지 않고, 1단계 개발 착수 전 PoC(실제 호출 테스트)로 먼저 확정한다. 이는 이후 모든 기능이 API 가용성에 좌우되는 핵심 의존성이기 때문이다.

## 15. 참고

본 문서는 [ideation.md](./ideation.md)의 내용을 제품 요구사항 형태로 재구성한 것이다. 아이디어 배경과 세부 검토 이력은 해당 문서를 참고한다.
