name: map
metadata:
  template: map@0.0.1-preview
services:
  backend:
    project: ./backend
    host: containerapp
    language: js
    docker:
      path: ./Dockerfile
      context: .
  frontend:
    project: ./frontend
    host: containerapp
    language: js
    docker:
      path: ./Dockerfile
      context: .
